name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
        type: string
      create_release:
        description: 'Create GitHub release'
        required: true
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Run tests
        run: dotnet test -c Release

      - name: Build installer
        run: dotnet build Installer/Installer.wixproj -c Release -p:AutoVersion=${{ inputs.version }}

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: Auto-${{ inputs.version }}-x64
          path: Installer/bin/Release/Auto-${{ inputs.version }}-x64.msi

      - name: Create GitHub Release
        if: inputs.create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          name: Auto v${{ inputs.version }}
          files: Installer/bin/Release/Auto-${{ inputs.version }}-x64.msi
          generate_release_notes: true
          draft: false
          prerelease: false
